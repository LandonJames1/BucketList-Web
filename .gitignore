# macOS
.DS_Store
._*

# Local Claude Code permissions — machine-specific, not project config
.claude/settings.local.json

# Editor / OS noise
*.swp
*~
Thumbs.db

# Nothing here is built, but just in case tooling ever lands
node_modules/
*.log
